interface CoverViewPage {
    createdAt: string;
    notFound: string;

    comment: {
        input: {
            placeholder: string;
            submit: string;
        }
    }
}

export default CoverViewPage;