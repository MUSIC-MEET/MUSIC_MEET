import SearchForm from "locales/interface/SearchForm";

const text: SearchForm = {
    input: {
        placeholder: "Input search keyword",
        submit: "Search"
    },
    select: {
        options: {
            title: "Title",
            nickname: "Nickname"
        }
    }
};

export default text;