import { css } from "@emotion/react";
import React from "react";

function ResultItem(props: SearchMusicType) {
    const { musicNum, imgSrc } = props;
    return (
        <div css={style}>

        </div>
    );
}

const style = css`
    
`;

export default ResultItem;